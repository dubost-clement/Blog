{% extends 'base.html.twig' %}
{% form_theme formCategory 'bootstrap_4_layout.html.twig' %}

{% block body %}
    <h1 class="text-center">Modifications reportage</h1>

    {{ form_start(formCategory) }}

        {{ form_row(formCategory.name, {'attr': {'placeholder': "Titre du reportage"}}) }}
        <button type="submit" class="btn btn-primary">Modifier</button>
        <a href="{{ path('category_list') }}" class="btn btn-primary">Retour</a>

    {{ form_end(formCategory) }}

{% endblock%}