{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-center">Gestion des catégories</h1>

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>nom</th>
            <th>actions</th>
        </tr>
    </thead>
    <tbody>
        {% for category in categories %}
            <tr>
                <td>{{ category.id }}</td>
                <td>{{ category.name }}</td>
                <td>
                    <a href="#">modifier</a>
                    <a href="#">Supprimer</a>
                </td>
            </tr>
        {% endfor %}
    </tbody>
</table>
<a href="{{ path('category_create') }}" class="btn btn-primary">Créer une catégories</a>
<a href="{{ path('admin') }}" class="btn btn-primary">Retour</a>

{% endblock %}